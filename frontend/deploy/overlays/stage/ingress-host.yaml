apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: freshcloud-ui
  namespace: freshcloud-ui
spec:
  tls:
    - hosts:
        - ui-stage.freshcloud.local
      secretName: freshcloud-ui-stage-tls
  rules:
    - host: ui-stage.freshcloud.local
