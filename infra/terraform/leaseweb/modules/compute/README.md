# Module: compute

Purpose:
- Provision Leaseweb Public Cloud instances from a typed host map.

Notes:
- The Leaseweb Public Cloud API/provider is beta; validate resource behavior in non-prod first.
- Keep `enable_instance_creation=false` until all Leaseweb assumptions are verified.
